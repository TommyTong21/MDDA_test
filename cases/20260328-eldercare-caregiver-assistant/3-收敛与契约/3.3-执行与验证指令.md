# 3.3 执行与验证指令（可直接跑）

> 本仓库当前未内置 `pnpm gate:*` 系列脚本；本文件提供可直接执行的验证与交付指令集合。

## 本地验证（Fast Lane）
```bash
pnpm lint:fast
pnpm build
pnpm test:e2e
```

## 本地开发
```bash
pnpm dev
```

## Supabase 迁移（手动执行）
在 Supabase Dashboard 的 SQL Editor 依次执行：
- `supabase/migrations/20260328000200_caregiver_mvp.sql`
- `supabase/migrations/20260328001000_caregiver_hardening.sql`

## 线上验证点（Vercel）
- `/api/version`
- `/`（首页）
- `/caregiver`（护工端 MVP）

